import type { Metadata } from 'next';
export const metadata: Metadata = { title: '设置 - 鸿一电竞', description: '管理你的账号和安全设置' };
export default function SettingsLayout({ children }: { children: React.ReactNode }) { return children; }
